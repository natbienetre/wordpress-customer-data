/**
 * Admin Settings Script
 *
 * @package
 * @version 1.0.0
 * @author Pierre Peronnet <pierre.peronnet@gmail.com>
 * @license GPL-2.0-or-later
 * @description This script manages the admin settings page for the VFS plugin.
 */

import './settings-page.scss';
/*
domReady( (): void => {
	const container: HTMLElement | null = document.getElementById(
		'vfs-swift-provider-openrc-loader'
	);
	if ( ! container ) {
		throw new Error( 'Container not found' );
	}

	// Add a button to load settings from an openrc file
	container.classList.add( 'vfs-admin-settings-load-openrc' );

	createRoot( container ).render(
		<LoadOpenrc
			authUrlInput={
				document.getElementById(
					'vfs-swift-auth-url'
				) as HTMLInputElement
			}
			identityApiVersionInput={
				document.getElementById(
					'vfs-swift-identity-api-version'
				) as HTMLInputElement
			}
			openstackUserDomainNameInput={
				document.getElementById(
					'vfs-swift-user-domain-name'
				) as HTMLInputElement
			}
			openstackTenantIdInput={
				document.getElementById(
					'vfs-swift-tenant-id'
				) as HTMLInputElement
			}
			openstackTenantNameInput={
				document.getElementById(
					'vfs-swift-tenant-name'
				) as HTMLInputElement
			}
			openstackPasswordInput={
				document.getElementById(
					'vfs-swift-password'
				) as HTMLInputElement
			}
			openstackUsernameInput={
				document.getElementById( 'vfs-swift-user' ) as HTMLInputElement
			}
			openstackRegionNameInput={
				document.getElementById(
					'vfs-swift-region'
				) as HTMLInputElement
			}
		/>
	);
} );
*/
